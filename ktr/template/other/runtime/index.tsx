import { defineTemplate } from '@karinjs/template-react'

import { RuntimeReport } from './components/runtime'

export default defineTemplate({
  name: '运行环境诊断',
  description: 'KKK插件运行环境诊断页面',
  component: RuntimeReport
})
