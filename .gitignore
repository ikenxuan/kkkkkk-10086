.vitepress/*
node_modules/*
config/config/*
data/*
yarn.lock/*
package-lock.json
!.gitignore
amagi/node_modules/*
.vscode/
# test/（单数）是早期的手动调试草稿，自带 node_modules，继续忽略。
# /tests/（复数）是 vitest + node --test 的正式测试树，从此跟踪，不再忽略：
# 它被忽略期间烂过两次而 CI 全绿（standalone 迁移烂了 11 处断言、
# 后来又烂了 4 个契约测试），因为 `pnpm test` 在全新检出上找不到用例就退 0。
# tests/ 不在 package.json 的 files 里，所以不会进 preview / master 发布分支。
test/
.eslintrc
/lib/
/.generated/
/.artifacts/
/.ktr/
/temp/
.claude/
graphify-out/
.graphifyignore
AGENTS.md
# docs/superpowers/ 是 agent 写方案时的草稿目录（writing-plans 那套），
# 和 .claude/ 、AGENTS.md 同类：只对当时那次改动有意义，进了仓库既没人读也会越堆越多。
# 它是在 0471487 那个大迁移提交里被整棵工作树一起扫进来的，不是有意提交。
# docs/ 下手写的文档（actions-local-validation.md）继续跟踪，所以只忽略这一层。
docs/superpowers/
